import { CalendarPage } from "@/components/calendar/CalendarPage";

export const metadata = {
  title: "Awards Calendar — The Cave",
  description: "Entry deadlines and eligibility windows across award shows.",
};

export default function Page() {
  return <CalendarPage />;
}
