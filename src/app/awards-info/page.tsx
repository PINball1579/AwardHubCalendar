import { AwardsInfoClient } from "@/components/awardsInfo/AwardsInfoClient";

export const metadata = {
  title: "Awards Info — The Cave",
  description: "Award show profiles, deadlines, and entry kits.",
};

export default function AwardsInfoPage() {
  return <AwardsInfoClient />;
}
